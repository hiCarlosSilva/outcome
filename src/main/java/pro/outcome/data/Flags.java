package pro.outcome.data;
import pro.outcome.util.KeyValue;


public abstract class Flags {

	public static Flag createFlag(String shortcut, String description) {
		Flag f = new Flag(shortcut, description);
		//if(_flags.)
		return f;
	}
}
